const state = {
    // general configuration state
    // see configuration/modules for particular states
    loading: {
        contextsRoutes: false,
    },
    saving: {
        contextsRoutes: false,
    },
}
export default state
